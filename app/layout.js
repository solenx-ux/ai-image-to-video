export const metadata = {
  title: "AI Image to Video",
  description: "AI Image to Video App"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
